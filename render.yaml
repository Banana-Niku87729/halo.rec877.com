services:
  - type: web
    name: ytdlp-backend
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn ytdlp_server:app --host 0.0.0.0 --port $PORT
    plan: free 
